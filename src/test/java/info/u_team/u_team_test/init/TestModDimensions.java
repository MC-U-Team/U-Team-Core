package info.u_team.u_team_test.init;

import net.minecraftforge.eventbus.api.IEventBus;

public class TestModDimensions {
	// TODO wait for forge replacement
	// public static final CommonDeferredRegister<ModDimension> MOD_DIMENSIONS =
	// CommonDeferredRegister.create(ForgeRegistries.MOD_DIMENSIONS, TestMod.MODID);
	//
	// public static final RegistryObject<ModDimension> BASIC = MOD_DIMENSIONS.register("basic", () ->
	// ModDimension.withFactory(BasicDimension::new));
	
	public static void register(IEventBus bus) {
		// MOD_DIMENSIONS.register(bus);
	}
}
